def find_x_times(my_lists, x):
    my_set = set()
    for list in my_lists:
        for element in list:
            my_set.add(element)

    for element in my_set:
        if element
    print(my_set)


find_x_times([[1, 2, 3], [2, 3, 4], [4, 5, 6], [4, 1, "test"]], 2)
